[build]
builder = "DOCKERFILE"
dockerfilePath = "/apps/web/Dockerfile"
watchPatterns = ["/apps/web/**", "/package.json", "/bun.lock", "/.dockerignore"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
