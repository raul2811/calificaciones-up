services:
  web:
    image: "ghcr.io/raul2811/calificaciones-up-web:${WEB_IMAGE_TAG:-main}"
    pull_policy: always
    depends_on:
      - api
    environment:
      HOSTNAME: 0.0.0.0
      PORT: 3000
      VITE_API_URL: "${VITE_API_URL:-http://localhost:8081}"
      VITE_APP_NAME: "${VITE_APP_NAME:-Calificaciones UP}"
      VITE_SITE_URL: "${VITE_SITE_URL:-http://localhost:3000}"
    ports:
      - "${WEB_PORT:-3000}:3000"

  api:
    image: "ghcr.io/raul2811/calificaciones-up-api:${API_IMAGE_TAG:-main}"
    pull_policy: always
    environment:
      PORT: 8081
      FRONTEND_ORIGIN: "${FRONTEND_ORIGIN:-http://localhost:3000}"
      RUST_LOG: "${RUST_LOG:-api=info,tower_http=info}"
      MATRICULA_BASE_URL: "${MATRICULA_BASE_URL:-https://matricula.up.ac.pa}"
      MATRICULA_USER_AGENT: "${MATRICULA_USER_AGENT:-Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36}"
    ports:
      - "${API_PORT:-8081}:8081"
